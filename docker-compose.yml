version: '3.8'

services:
  app:
    build: 
      context: .
      target: builder
    ports:
      - "4321:4321"
    environment:
      - NODE_ENV=development
    volumes:
      - .:/app
      - /app/node_modules
    command: pnpm run dev